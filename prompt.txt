riješi sve zadatke koji su traženi u pdfu. odijeli zadatke tako da napišeš npr. #1. zadatak te podzadatke #1.a). kod komentara i ostalih dijelova (npr title) ne koristi broj zadatak u pdfu npr. 0.0.2 te ne koristi nizove crtica npr "---". svi grafovi moraju imati title i imenovane x_label te y_label. matrice koje se prikazuju s plt uvijek moraju imati upisane vrijednosti u kvadrate. callbacks piši kao: npr. callbacks = [] i onda pozovi samo callbacks=callbacks kod .fit. pohrana modela na disk neka je npr. model.save("model.keras"). kod treniranja mreža, dovoljno je samo npr. model.fit(...
